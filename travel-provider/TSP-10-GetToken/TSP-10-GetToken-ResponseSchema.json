{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "TSP-10-GetToken Response Schema",
  "version": "0.0.1",
  "description": "Schema for authentication token response provided to RMP by the TSP",
  "properties": {
    "access_token": {
      "type": "string",
      "description": "The authentication token."
    },
    "token_type": {
      "type": "string",
      "description": "The type of token provided.",
      "enum": [
        "Bearer"
      ]
    },
    "issued_at": {
      "type": "integer",
      "description": "Timestamp when the token was generated by the TSP. Formatted as a Unix timestamp in seconds."
    },
    "expires_in": {
      "type": "integer",
      "description": "Number of seconds until the issued token expires."
    }
  }
}